<p id="notice"><%= notice %></p>

<h1>Events Users</h1>

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events_users.each do |events_user| %>
      <tr>
        <td><%= events_user.event_id %></td>
        <td><%= events_user.user_id %></td>

    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Events User', new_events_user_path %>
