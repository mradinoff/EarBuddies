<%= form_with(model: events_user, local: true) do |form| %>
  <% if events_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(events_user.errors.count, "error") %> prohibited this events_user from being saved:</h2>

      <ul>
      <% events_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id, id: :events_user_event_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :events_user_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
